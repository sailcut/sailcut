language: cpp
cache: apt
env:
  - QT_SELECT=qt5
install:
  - sudo add-apt-repository -y ppa:ubuntu-sdk-team/ppa
  - sudo apt-get update
  - sudo apt-get install qtbase5-dev qttools5-dev-tools libqt5opengl5-dev libqt5svg5-dev
script:
  - qmake
  - make
